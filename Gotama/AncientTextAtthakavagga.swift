import Foundation

enum AncientTextAtthakavagga {
    /// Returns the content of the Atthakavagga (Snp 4)
    /// This is stored separately from the main AncientText enum for better code organization
    static var content: String {
        return """
        [Placeholder for Atthakavagga (Snp 4) content]
        """
    }
} 